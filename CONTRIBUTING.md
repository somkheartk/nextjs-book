# การมีส่วนร่วม (Contributing)

ยินดีต้อนรับผู้ที่ต้องการมีส่วนร่วมในการพัฒนาหนังสือเล่มนี้! 🎉

## 🤝 วิธีการมีส่วนร่วม

### 1. รายงานข้อผิดพลาด (Bug Report)

หากพบข้อผิดพลาดหรือข้อความที่ไม่ถูกต้อง:

1. ตรวจสอบว่ามีคนรายงานไปแล้วหรือยัง (ใน Issues)
2. เปิด Issue ใหม่พร้อมระบุ:
   - บทหรือไฟล์ที่พบปัญหา
   - รายละเอียดของปัญหา
   - ภาพหน้าจอ (ถ้ามี)
   - วิธีแก้ไขที่แนะนำ (ถ้ามี)

### 2. เสนอแนะฟีเจอร์ใหม่ (Feature Request)

มีไอเดียดีๆ? แชร์กับเรา!

1. เปิด Issue ใหม่
2. ใส่ label `enhancement`
3. อธิบายฟีเจอร์ที่ต้องการ
4. ให้เหตุผลว่าทำไมควรเพิ่ม

### 3. ส่ง Pull Request

#### ขั้นตอน:

1. Fork repository นี้
2. สร้าง branch ใหม่:
   ```bash
   git checkout -b feature/your-feature-name
   ```
3. ทำการแก้ไข
4. Commit changes:
   ```bash
   git commit -m "Add: your description"
   ```
5. Push ไปยัง branch:
   ```bash
   git push origin feature/your-feature-name
   ```
6. เปิด Pull Request

#### Commit Message Guidelines:

- `Add:` - เพิ่มฟีเจอร์หรือเนื้อหาใหม่
- `Fix:` - แก้ไขข้อผิดพลาด
- `Update:` - อัปเดตเนื้อหา
- `Refactor:` - ปรับปรุงโค้ด
- `Docs:` - แก้ไขเอกสาร

ตัวอย่าง:
```
Add: Chapter 11 - Advanced Patterns
Fix: Typo in Chapter 3, section 2
Update: Improve code examples in Lab 5
```

## 📝 แนวทางการเขียน

### ภาษา

- ใช้ภาษาไทยที่เข้าใจง่าย
- หลีกเลี่ยงคำศัพท์เทคนิคมากเกินไป
- แปลศัพท์เทคนิคเป็นไทย แต่ควรใส่ภาษาอังกฤษในวงเล็บ

ตัวอย่าง:
```markdown
✅ ดี: "State (สถานะ) คือข้อมูลที่เปลี่ยนแปลงได้"
❌ ไม่ดี: "State is mutable data"
```

### โครงสร้าง

แต่ละบทควรมี:

1. **🎯 สิ่งที่จะได้เรียนรู้** - สรุปย่อ
2. **เนื้อหา** - อธิบายแนวคิด
3. **ตัวอย่างโค้ด** - ตัวอย่างที่ใช้งานได้จริง
4. **✅ สรุป** - สรุปสิ่งที่เรียนรู้
5. **🔗 Lab** - Link ไปยัง Lab ที่เกี่ยวข้อง
6. **📝 แบบฝึกหัด** - แบบฝึกหัดเพิ่มเติม
7. **🎯 เป้าหมายต่อไป** - บทถัดไป

### ตัวอย่างโค้ด

- ควรสั้น กระชับ และเข้าใจง่าย
- มี comment อธิบาย (ภาษาไทย)
- ใช้งานได้จริง (ทดสอบแล้ว)
- Format ให้สวยงาม

```javascript
// ✅ ดี
export default function Button({ text, onClick }) {
  return (
    <button 
      onClick={onClick}
      className="px-4 py-2 bg-blue-600 text-white rounded"
    >
      {text}
    </button>
  )
}

// ❌ ไม่ดี (ไม่มี comment, format ไม่ดี)
export default function Button({text,onClick}){return <button onClick={onClick} className="px-4 py-2 bg-blue-600 text-white rounded">{text}</button>}
```

## 🎨 Code Style

### Markdown

- ใช้ headings อย่างถูกต้อง (# > ## > ###)
- เว้นบรรทัดระหว่างส่วนต่างๆ
- ใช้ emoji เพื่อความชัดเจน (🎯 📚 ✅)

### JavaScript/React

- ใช้ Functional Components
- ใช้ ES6+ syntax
- ตั้งชื่อตัวแปรให้เข้าใจง่าย
- ไม่มี console.log ในโค้ดตัวอย่าง

## 🧪 Testing

ก่อนส่ง PR:

- [ ] อ่านเนื้อหาทั้งหมด
- [ ] ทดสอบโค้ดตัวอย่างทุกอัน
- [ ] ตรวจสอบ Markdown formatting
- [ ] ตรวจสอบ typos
- [ ] ตรวจสอบ links

## 📋 Checklist สำหรับ PR

- [ ] ทดสอบโค้ดแล้ว
- [ ] ไม่มี typos
- [ ] Format ถูกต้อง
- [ ] มี commit message ที่ดี
- [ ] อธิบาย changes ใน PR description

## ❓ คำถาม

หากมีคำถามหรือต้องการความช่วยเหลือ:

- เปิด Issue
- ถาม in Discussions
- ติดต่อผู้ดูแล

## 📜 Code of Conduct

- เคารพผู้อื่น
- ให้ feedback แบบสร้างสรรค์
- ยอมรับความคิดเห็นที่แตกต่าง
- ช่วยเหลือผู้เริ่มต้น

## 🙏 ขอบคุณ

ขอบคุณทุกท่านที่มีส่วนร่วมในการพัฒนาหนังสือเล่มนี้!

การมีส่วนร่วมของคุณช่วยให้หนังสือเล่มนี้ดีขึ้นสำหรับผู้เรียนทุกคน 💙

---

**Happy Contributing! 🎉**
