# การจัดการหนังสือ Next.js

คู่มือนี้อธิบายวิธีจัดการไฟล์และสร้างเอกสารรูปแบบต่างๆ

## 📁 โครงสร้างโปรเจค

```
nextjs-book/
├── README.md                    # หน้าแรกและสารบัญหลัก
├── nextjs-book-combined.md      # หนังสือรวมทุกบทในไฟล์เดียว
├── chapters/                    # บทเรียนทั้งหมด
│   ├── 01-introduction/
│   ├── 02-pages-routing/
│   └── ...
├── labs/                        # แบบฝึกหัด
├── scripts/                     # สคริปต์สำหรับสร้างไฟล์
│   └── combine-markdown.js      # สร้างไฟล์รวม
└── package.json                 # การตั้งค่าโปรเจค
```

## 🔄 การสร้างไฟล์รวม (Combined Markdown)

หากมีการแก้ไขเนื้อหาในบทใดบท สามารถสร้างไฟล์ `nextjs-book-combined.md` ใหม่ได้:

```bash
# วิธีที่ 1: ใช้ npm script
npm run combine:md

# วิธีที่ 2: รันสคริปต์โดยตรง
node scripts/combine-markdown.js
```

สคริปต์จะ:
1. อ่านไฟล์ README.md หลัก
2. รวมทุกบทจาก `chapters/` ตามลำดับ
3. เพิ่ม GLOSSARY และ QUICK_REFERENCE (ถ้ามี)
4. สร้างไฟล์ `nextjs-book-combined.md`

## 📄 การแปลงเป็น PDF

### วิธีที่ 1: Pandoc (คุณภาพสูงสุด)

```bash
# ติดตั้ง Pandoc
# Ubuntu/Debian
sudo apt-get install pandoc texlive-xetex texlive-fonts-extra

# macOS
brew install pandoc basictex

# สร้าง PDF พร้อมฟอนต์ไทย
pandoc nextjs-book-combined.md \
  -o nextjs-book-th.pdf \
  --pdf-engine=xelatex \
  -V mainfont="Sarabun" \
  -V geometry:margin=2cm \
  --toc \
  --toc-depth=2 \
  -V documentclass=report
```

### วิธีที่ 2: ใช้เว็บไซต์

อัปโหลด `nextjs-book-combined.md` ไปที่:
- [Markdown to PDF](https://www.markdowntopdf.com/)
- [CloudConvert](https://cloudconvert.com/md-to-pdf)
- [PDF Candy](https://pdfcandy.com/md-to-pdf.html)

### วิธีที่ 3: VS Code Extension

1. ติดตั้ง extension "Markdown PDF" (yzane.markdown-pdf)
2. เปิดไฟล์ `nextjs-book-combined.md`
3. กด `Ctrl+Shift+P` แล้วเลือก "Markdown PDF: Export (pdf)"

## ✏️ การแก้ไขเนื้อหา

### แก้ไขบทเรียน

1. เข้าไปแก้ไขในโฟลเดอร์ `chapters/[chapter-name]/README.md`
2. สร้างไฟล์รวมใหม่: `npm run combine:md`
3. ตรวจสอบการเปลี่ยนแปลง: `git diff nextjs-book-combined.md`

### แก้ไข README หลัก

แก้ไขไฟล์ `README.md` โดยตรง - ไม่ต้องรันสคริปต์

### เพิ่มบทใหม่

1. สร้างโฟลเดอร์ใหม่ใน `chapters/` เช่น `11-new-chapter/`
2. สร้างไฟล์ `README.md` ในโฟลเดอร์นั้น
3. แก้ไข `scripts/combine-markdown.js`:
   - เพิ่มชื่อบทใน array `chapters`
   - เพิ่มชื่อไทยใน function `getChapterTitle`
4. อัพเดท `README.md` หลัก
5. รัน `npm run combine:md`

## 🎨 ปรับแต่ง PDF

หากต้องการปรับแต่งรูปแบบ PDF:

### ปรับ margins และขนาดกระดาษ

```bash
pandoc nextjs-book-combined.md \
  -o nextjs-book-th.pdf \
  --pdf-engine=xelatex \
  -V geometry:a4paper \
  -V geometry:margin=3cm
```

### เพิ่ม custom CSS (สำหรับ HTML output)

```bash
pandoc nextjs-book-combined.md \
  -o nextjs-book-th.html \
  --css=custom-styles.css \
  --standalone
```

### สร้าง EPUB

```bash
pandoc nextjs-book-combined.md \
  -o nextjs-book-th.epub \
  --toc \
  --epub-cover-image=cover.jpg
```

## 📊 สถิติหนังสือ

ตรวจสอบขนาดเนื้อหา:

```bash
# นับจำนวนบรรทัด
wc -l nextjs-book-combined.md

# นับจำนวนคำ
wc -w nextjs-book-combined.md

# นับจำนวนอักษร
wc -c nextjs-book-combined.md

# แสดงข้อมูลทุกอย่าง
wc nextjs-book-combined.md
```

## 🔍 ตรวจสอบลิงก์

ตรวจสอบว่าลิงก์ใน README ใช้งานได้:

```bash
# ค้นหาลิงก์ทั้งหมด
grep -o '\[.*\](.*\.md)' README.md

# ตรวจสอบว่าไฟล์ที่ลิงก์มีอยู่จริง
for file in chapters/*/README.md; do
  [ -f "$file" ] && echo "✓ $file" || echo "✗ $file missing"
done
```

## 🚀 Tips

1. **ใช้ Git**: Commit เนื้อหาเป็นประจำ
2. **Preview**: ใช้ GitHub หรือ VS Code preview markdown ก่อน export PDF
3. **ฟอนต์ไทย**: ต้องมีฟอนต์ไทยติดตั้งในระบบเพื่อแสดงผลถูกต้อง (Sarabun, Tahoma, หรือ Angsana)
4. **ขนาดไฟล์**: ไฟล์ `nextjs-book-combined.md` มีประมาณ 5000 บรรทัด
5. **การอัพเดท**: รัน `npm run combine:md` ทุกครั้งที่แก้ไขเนื้อหาในบท

## 📝 License

MIT License - สามารถนำไปใช้และแก้ไขได้อย่างอิสระ
